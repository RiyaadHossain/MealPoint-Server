/**
 * Fields that can be used to filter users
 */
export const userFilterableFields = ["searchTerm", "role", "email"];

/**
 * Fields that can be searched in users
 */
export const userSearchableFields = ["name", "email"];
