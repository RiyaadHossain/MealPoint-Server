export enum UserRole {
  CUSTOMER = "customer",
  ADMIN = "admin",
}

export enum UserLevel {
  BRONZE = "bronze",
  SILVER = "silver",
  GOLD = "gold",
  PLATINUM = "platinum",
}

export enum AuthProvider {
  GOOGLE = "google",
  FACEBOOK = "facebook",
  GITHUB = "github",
  LOCAL = "local",
}
